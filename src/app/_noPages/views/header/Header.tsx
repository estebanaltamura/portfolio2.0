import { Box } from '@mui/material';
import { headerHeight } from '../../config/layoutConfig';

const Header = () => {
  return <Box sx={{ height: `${headerHeight}px` }}></Box>;
};

export default Header;
