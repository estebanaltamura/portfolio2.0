import { Box } from '@mui/material';

const Projects = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',
        maxWidth: '100%',
        height: '649px',
        border: '1px solid #494949',
        borderRadius: '20px',
        marginTop: '60px',
      }}
    ></Box>
  );
};

export default Projects;
