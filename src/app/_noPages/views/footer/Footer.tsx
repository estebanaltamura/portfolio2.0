import { Box } from '@mui/material';
import { footerHeight } from '../../config/layoutConfig';

const Footer = () => {
  return <Box sx={{ height: `${footerHeight}px` }}></Box>;
};

export default Footer;
